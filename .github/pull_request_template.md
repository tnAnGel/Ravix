<!-- Thanks for the patch. Keep this short — a few honest lines beat a filled-in form. -->

## What this changes

<!-- One paragraph. If it fixes an issue, write "Fixes #123". -->

## Why

<!-- The problem it solves, not a restatement of the diff. -->

## How it was tested

<!-- Commands you ran, and what you exercised manually. -->

- [ ] `npm run lint` and `npm test` pass
- [ ] `cd backend && mvn verify` passes
- [ ] User-facing strings added to **both** `src/i18n/ru.ts` and `src/i18n/en.ts`
- [ ] Schema changes ship as a new Flyway migration (no edits to applied ones)
- [ ] `docs/en/` updated if behaviour changed
